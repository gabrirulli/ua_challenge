class CurrencyExchange < ApplicationRecord
  # Tabella usata per salvarsi le varie lingue disponibili, le loro valute ed i loro tassi di scambio
  # In questo caso si creeranno solo due record con country_code: "es" e "es_us" (vedi db/seeds.rb)
end
